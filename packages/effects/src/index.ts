/**
 * @moora/effects
 *
 * 两阶段副作用系统，用于管理状态机中的副作用。
 *
 * Effect 是一个两阶段副作用函数：
 * - 第一阶段（同步）：调用 Effect 函数本身，返回一个异步副作用函数
 * - 第二阶段（异步）：异步副作用函数在微任务队列中执行
 *
 * @packageDocumentation
 */

// ============================================================================
// 导出类型
// ============================================================================
export type { Effect } from './types';

// ============================================================================
// 导出函数
// ============================================================================
export {
  runEffect,
  noopEffect,
  synchronous,
  asynchronous,
  stateful,
  parallel,
  sequential,
} from './effect';
